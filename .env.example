# Server
PORT=5174
DATA_DIR=./data
DB_FILE=./data/tournament.db

# Admin panel password (required to access /admin routes)
# Change this! Default is "admin" if not set.
ADMIN_PASSWORD=admin

# Client -> server URL (optional; dev can use same-origin proxy)
VITE_SOCKET_URL=http://localhost:5174
